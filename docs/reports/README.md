# Reports

This directory is reserved for final analysis documents and standalone
writeups.

Use `reports/{topic}-{date}.md` or `reports/{topic}-summary.md` when an
investigation is complete and the useful artifact is the conclusion, not the
full working diary. Multi-day active investigations belong in `../tasks/`.
